# SKRSI agent instructions

SKRSI means **SK Recursive SELF Improvement**. SELF means **Systematic Evaluation, Learning, and Feedback**.

- Work only through an eligible, explicitly claimed SKCapstone card.
- Keep SKRSI scoped to SKDashboard, SKWorld, and shared software lifecycle orchestration. Do not assign it an SKLegal product role.
- Preserve lowercase `skrsi` runtime, schema, metric, and evidence identifiers.
- Models and agents produce typed proposals. They do not own authorization, workflow state, merge, deployment, release, or actuation.
- Require distinct producer and reviewer identities.
- Keep queues, retries, timeouts, concurrency, and evidence bounded.
- Preserve missingness, uncertainty, negative findings, and historical wording in immutable evidence.
- Do not introduce a human gate unless current authorization cannot be established safely. Notify humans for ordinary outcomes.
- Use SKMail at startup, before major handoffs, and before completion.
- Use `rg` for search and `apply_patch` for edits.
- Never use an em dash or en dash.
- Do not merge or deploy unless the card explicitly authorizes it.
